<div id="cookie-consent" class="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 shadow-lg transform translate-y-full transition-transform duration-500 z-50">
    <div class="container mx-auto px-4 py-4 flex flex-col md:flex-row items-center justify-between">
        <div class="mb-4 md:mb-0 pr-4">
            <p class="text-gray-700 text-sm md:text-base">
                This site uses cookies to improve your experience.
                <a href="{{ url_for('privacy-policy') }}" class="text-black font-medium hover:underline">Learn more</a>
            </p>
        </div>
        <div class="flex flex-col sm:flex-row gap-3">
            <button id="cookie-consent-reject" class="px-4 py-2 bg-transparent border border-gray-300 text-gray-700  hover:bg-gray-100 transition-colors text-sm">
                Reject
            </button>
            <button id="cookie-consent-accept" class="px-4 py-2 bg-yellow-400 text-black font-medium  hover:bg-yellow-500 transition-colors text-sm">
                Accept cookies
            </button>
        </div>
    </div>
</div>